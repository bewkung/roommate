<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="css/header.css">
    <link rel="stylesheet" href="css/layout.css">
    <title>เพิ่มข้อมูลห้อง</title>
</head>

<body class="bg-gray-100 min-h-screen flex flex-col">

  
    <%- include('partials/homeheader') %>

    <!-- Main Content -->
    <div class="flex justify-center mt-6">
        <div class="w-full max-w-2xl bg-white rounded-lg shadow-md p-6">

            <!-- Title -->
            <h1 class="text-2xl font-bold text-center mb-6">
                เพิ่มข้อมูลห้องของคุณ
            </h1>

            <!-- Form -->
            <form action="/room" method="POST" enctype="multipart/form-data" class="space-y-4">

                <!-- Room Name -->
                <div>
                    <label class="block mb-1 font-semibold">ชื่อห้อง</label>
                    <input
                        type="text"
                        name="room_name"
                        placeholder="เช่น ห้องพัก B201"
                        class="w-full border rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
                        required
                    >
                </div>

                <!-- Room Description -->
                <div>
                    <label class="block mb-1 font-semibold">รายละเอียดห้อง</label>
                    <textarea
                        name="room_description"
                        rows="4"
                        placeholder="ระบุรายละเอียดเกี่ยวกับห้อง"
                        class="w-full border rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
                        required
                    ></textarea>
                </div>

                <!-- Max Capacity -->
                <div>
                    <label class="block mb-1 font-semibold">จำนวนคนสูงสุด</label>
                    <input
                        type="number"
                        name="Max_quantity"
                        max="4"
                        class="w-full border rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
                        required
                    >
                </div>

                <!-- Current Quantity -->
                <div>
                    <label class="block mb-1 font-semibold">จำนวนคนปัจจุบัน</label>
                    <input
                        type="number"
                        name="quantity"
                        min="0"
                        class="w-full border rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
                        required
                    >
                </div>

                <!-- Room Image -->
                <div>
                    <label class="block mb-1 font-semibold">อัปโหลดรูปภาพห้อง</label>
                    <input
                        type="file"
                        name="profileImage"
                        accept="image/*"
                        class="w-full border rounded-md px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
                    >
                </div>

                <!-- Submit Button -->
                <div class="text-center">
                    <button
                        type="submit"
                        class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-2 rounded-lg shadow-md"
                    >
                        บันทึกข้อมูล
                    </button>
                </div>
            </form>
        </div>
    </div>

</body>
</html>

